http {

	client_max_body_size	10M;

	keepalive_timeout 65;

	server {
		listen 9000;
		server_name localhost;
		root content/www;
		
		autoindex on; #wtf

		location / {
				limit_except GET POST
				deny { all;
			}
		}
	}
}
